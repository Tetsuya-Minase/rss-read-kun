# rss-read-kun
Get the RSS feed and notify via discord.

# How to use
```
$ GEMINI_API_KEY=`your api key` GEMINI_MODEL=`gemini-3.8-flash` DISCORD_WEBHOOK_URL=`your discord webhook url` cargo run
$ curl localhost:8080/
```

# License
MIT
